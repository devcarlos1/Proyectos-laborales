{
  "scripts": {
    "test": "npm run lint && mocha test/run.js",
    "lint": "./node_modules/.bin/stylelint \"**/*.scss\" --syntax scss",
    "start": "",
    "sass": "sass"

  },
  "dependencies": {
    "inuitcss": "^6.0.0",
    "sass": "^1.58.3"
  }
}
